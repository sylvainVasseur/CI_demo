name: Toto
on: [push]
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Github  #variable de contexte disponible
        run: echo '${{ toJSON(github) }}'
      - name: job
        run: echo '${{ toJSON(job) }}'
      - name: steps
        run: echo '${{ toJSON(steps) }}'
      - name: Runner
        run: echo '${{ toJSON(runner) }}'
      
